name: RunenSDF Validation

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: runen-sdf-validation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate standalone framework
    uses: dornglut/github-workflows/.github/workflows/reusable-rust-cargo-validate.yml@68cf60784b26bde0e32a30c1e06aed0311cc4b89
